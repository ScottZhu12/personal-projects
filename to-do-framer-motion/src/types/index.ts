export type TodosListType = {
  id: string;
  title: string;
  status: string;
  time: string;
};

export type newTodoType = {
  title: string;
  status: string;
};

export type updateTodoType = {
  id: string;
  title: string;
  status: string;
};
